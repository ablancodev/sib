package core;

import java.util.*;

public class TablaSimbolosOld {
/*
	HashMap table;

	public TablaSimbolos() {
		table = new HashMap();
	}

	public Simbolo add( Simbolo s ) {
		table.put( s.getName(), s );
		return s;
	}

	public Simbolo get( String name ) {
		return (Simbolo)( table.get( name ) );
	}

	public void print() {
		Iterator<Simbolo> it = table.values().iterator();
		while (it.hasNext()) {
			Simbolo s = it.next();
			System.out.println( s );
		}
	}
*/
}